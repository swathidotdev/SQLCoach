"""Reporting layer.

Aggregates analysis and recommendation results into human-readable
(HTML/CLI) and machine-readable (JSON) reports, and hosts higher-level
orchestration services such as `compare` and `audit`. Introduced
starting Sprint 9.
"""
